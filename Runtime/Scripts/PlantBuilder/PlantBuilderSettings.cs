using UnityEngine;

namespace Luzzi.PlantSystem
{
    // ScriptableObject pour la config globale du gizmo
    public class PlantBuilderSettings : ScriptableObject
    {
        public float gizmoSize = 1f;
        public bool showGizmo = true;
    }
}
